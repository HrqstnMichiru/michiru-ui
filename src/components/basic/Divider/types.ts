export interface DividerProps {
    dashed?: boolean; // 是否为虚线
    thickness?: number; // 线条粗细
    color?: string; // 线条颜色
    margin?: number; // 线条外边距
    length?: number; // 线条长度
    vertical?: boolean; // 是否为垂直分割线
}
